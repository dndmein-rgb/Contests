/*
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗓️ Weekly Contest 477
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 Problem: Q3754. Concatenate Non-Zero Digits and Multiply by Sum I
📌 Platform: LeetCode
🏷️ Difficulty: Easy
🏆 Topics: Math, Simulation, Digit Manipulation

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📖 Problem Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You are given an integer `n`. You need to:

1. Form a new integer `x` by concatenating all **non-zero** digits of `n` in their original order.
2. If there are no non-zero digits, `x = 0`.
3. Calculate `sum`, which is the sum of the digits in `x`.
4. Return the value of `x * sum`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 Approach
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The core challenge is building the number `x` without strings while keeping the original order. We process digits from right to left (using modulo 10) and reconstruct the number using a positional multiplier.

Steps:
1. Initialize `ans = 0` (to store `x`), `sum = 0`, and `final = 1` (the power of 10).
2. While `n > 0`:
   * Extract the last digit: `digit = n % 10`.
   * If `digit != 0`:
     * Add `digit` to `sum`.
     * Update `ans` by placing the current digit at the correct decimal position: `ans = (digit * final) + ans`.
     * Multiply `final` by 10 for the next non-zero digit.
   * Divide `n` by 10 to move to the next digit.
3. Return the result of `ans * sum`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🧠 Algorithm Walkthrough
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Example: `n = 1024`

1.  **Digit 4**: Non-zero.
    * `sum = 4`
    * `ans = (4 * 1) + 0 = 4`
    * `final = 10`
2.  **Digit 2**: Non-zero.
    * `sum = 4 + 2 = 6`
    * `ans = (2 * 10) + 4 = 24`
    * `final = 100`
3.  **Digit 0**: Zero. Skip.
4.  **Digit 1**: Non-zero.
    * `sum = 6 + 1 = 7`
    * `ans = (1 * 100) + 24 = 124`

Final Calculation: `124 * 7 = 868`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚙️ Implementation
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*/

class Solution {
public:
    long long sumAndMultiply(int n) {
        long long ans = 0;
        int sum = 0;
        long long final = 1; 

        while (n > 0) {
            int digit = n % 10;
            if (digit != 0) {
                sum += digit;
                // Build the number from right to left maintaining order
                ans = (long long)digit * final + ans;
                final *= 10;
            }
            n /= 10;
        }
        
        return ans * sum;
    }
};

/*
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚡ Complexity Analysis
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Let `d` be the number of digits in the integer `n`.

Time Complexity:
* **O(d)**: We iterate through the digits of `n` exactly once. For a 32-bit integer, `d` is at most 10.

Space Complexity:
* **O(1)**: We use a fixed amount of extra space for variables regardless of the size of `n`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎓 Key Notes
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

* **Overflow Prevention**: Using `long long` for `ans` and the return type is critical because concatenating digits can quickly exceed the range of a standard 32-bit `int`.
* **Zero Handling**: If `n` is `0` or only contains `0`s (e.g., `000`), the logic correctly results in `0`.
* **Mathematical Reconstruction**: Using `digit * final + ans` is a clever way to prepend digits to a number without using string manipulation or reversing an array.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*/
